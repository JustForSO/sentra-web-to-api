# 环境变量示例（请按需复制为 .env 并填写）
# 说明：所有值均为可选。未配置时使用代码内默认值。

# ==================== 代理配置（Proxy）====================
# 优先级：AI_PROXY_URL > HTTPS_PROXY > HTTP_PROXY > ALL_PROXY
# NO_PROXY（或 no_proxy）用于配置不走代理的域名，逗号分隔。
# 示例（HTTP/HTTPS/SOCKS5 均可，按你的代理工具给出的 URL 来填）：
# AI_PROXY_URL=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890
# HTTP_PROXY=http://127.0.0.1:7890
# ALL_PROXY=socks5://127.0.0.1:1080
# NO_PROXY=localhost,127.0.0.1,.example.com

# ==================== 重试与超时（Retries & Timeouts）====================
# 针对通用请求/编排器的默认配置，可被代码局部覆盖
# 最大重试次数（单次请求的失败重试次数）
# AI_RETRY_MAX_RETRIES=2
# 每次重试的基础延迟（毫秒）
# AI_RETRY_DELAY_MS=500
# 聊天编排器的最大总尝试次数（含切换模型的重试）
# AI_RETRY_MAX_TOTAL_ATTEMPTS=3
# 默认请求超时时间（毫秒）
# AI_DEFAULT_TIMEOUT_MS=30000

# ==================== 日志等级（Logging）====================
# 可选：debug, info, warn, error
# AI_LOG_LEVEL=info

# ==================== 消息处理配置（Message Processing）====================
# 是否启用reasoning_content字段转换，默认开启
# 开启后会从<think>标签中提取思考内容到reasoning_content字段
# ENABLE_REASONING_CONTENT=true

# ==================== 服务器访问控制（Server Access Control）====================
# 服务器访问令牌，用于API认证（必填）
# 客户端需要在请求头中包含：Authorization: Bearer <ACCESS_TOKEN>
ACCESS_TOKEN=your_secret_access_token_here

# 服务器端口配置
# PORT=7799