# Chatai Provider Configuration
# This provider uses fixed tokens and machine IDs, no additional configuration needed

# Optional: Custom machine ID (if not set, will generate random one)
# CHATAI_MACHINE_ID=your_custom_machine_id

# Optional: Custom token (if not set, will use default)
# CHATAI_TOKEN=your_custom_token

# Note: Chatai provider works without authentication tokens
# The provider uses fixed machine ID and token for API access
