# 获取方式：登录 https://www.liblib.art/ → F12
LIBLIB_TOKEN=your_liblib_token_here

# 模型检查点ID - 指定使用的AI绘图模型
# 不同的模型适合不同的绘画风格
# 可在LibLib网站的模型页面找到对应的ID
LIBLIB_CHECKPOINT_ID=your_checkpoint_id_here

# 图像宽度 - 生成图像的像素宽度 (建议：512-1536)
# 较大尺寸需要更多计算资源和时间
LIBLIB_WIDTH=1024

# 图像高度 - 生成图像的像素高度 (建议：512-1536)
# 常用比例：1:1 (1024x1024), 4:3 (1024x768), 16:9 (1024x576)
LIBLIB_HEIGHT=1288

# 生成图像数量 - 一次请求生成的图像数量 (1-4)
LIBLIB_IMG_COUNT=1

# CFG Scale - 提示词遵循程度 (1-30, 推荐：7-12)
# 数值越高越严格遵循提示词，但可能降低创意性
LIBLIB_CFG_SCALE=7

# 随机种子 - 控制生成的随机性 (-1为随机)
# 使用相同种子和参数可以复现相同结果
# -1: 随机种子, 正整数: 固定种子
LIBLIB_SEED=-1

# 额外种子 - 辅助随机种子，用于微调生成效果
LIBLIB_SEED_EXTRA=0

# 采样方法 - 影响图像生成的算法（使用数字选择，下列清单自上而下依次对应 1..28）
#  1: Euler a
#  2: Euler
#  3: LMS
#  4: Heun
#  5: DPM2
#  6: DPM2 a
#  7: DPM++ 2S a
#  8: DPM++ 2M
#  9: DPM++ SDE
# 10: DPM++ fast
# 11: DPM++ adaptive
# 12: LMS Karras
# 13: DPM2 Karras
# 14: DPM2 a Karras
# 15: DPM++ 2S a Karras
# 16: DPM++ 2M Karras
# 17: DPM++ SDE Karras
# 18: DDIM
# 19: PLMS
# 20: UniPC
# 21: DPM++ 2M SDE Karras
# 22: DPM++ 2M SDE Exponential
# 23: DPM++ 2M SDE Heun Karras
# 24: DPM++ 2M SDE Heun Exponential
# 25: DPM++ 3M SDE Karras
# 26: DPM++ 3M SDE Exponential
# 27: Restart
# 28: LCM
# 建议：7 (DPM++ 2S a) 或 8 (DPM++ 2M) 质量与速度均衡
LIBLIB_SAMPLING_METHOD=6

# 采样步数 - 生成迭代次数 (10-150, 推荐：20-50)
LIBLIB_SAMPLING_STEP=20

# CLIP跳过层数 - 影响提示词理解 (1-12, 推荐：1-2)
# 数值越高越注重艺术风格，越低越注重细节描述
LIBLIB_CLIP_SKIP=2

# 随机数生成源 - 控制随机性来源
# 0: CPU, 1: GPU
LIBLIB_RANDN_SOURCE=1

# 负面提示词 - 指定不希望出现的元素
LIBLIB_NEGATIVE_PROMPT=ng_deepnegative_v1_75t,(badhandv4:1.2),EasyNegative,(worst quality:2),lowres,bad anatomy,bad hands,text,error,missing fingers,extra digit,fewer digits,cropped,jpeg artifacts,signature,watermark,username,blurry

# 精炼器 - 二次处理提升图像质量 (0=关闭, 1=开启)
# 开启后会增加生成时间但提升细节质量
LIBLIB_REFINER=0

# 面部修复 - 自动修复面部细节 (0=关闭, 1=开启)
LIBLIB_RESTORE_FACES=0

# 高分辨率修复 - 提升图像分辨率 (0=关闭, 1=开启)
# 先生成小图再放大，可获得更高质量的大图
LIBLIB_HIRES_FIX=0

# 平铺模式 - 生成可平铺的纹理图案 (0=关闭, 1=开启)
# 适用于制作背景纹理和重复图案
LIBLIB_TILING=0

# ADetailer自动细节增强 - 自动检测和优化细节 (0=关闭, 1=开启)
# 可自动识别和修复人脸、手部等细节
LIBLIB_ADETAILER_ENABLE=0

# 提示词魔法增强 - 自动优化提示词 (0=关闭, 1=开启)
# 系统会自动添加质量提升词汇
LIBLIB_PROMPT_MAGIC=0

LIBLIB_GENERATE_TYPE=1

# 最大轮询次数 - 查询生成状态的最大次数 (10-120)
# 防止长时间等待，超过次数将超时失败
LIBLIB_MAX_ATTEMPTS=60

# 轮询间隔 - 每次查询间隔时间，毫秒 (3000-10000)
# 间隔太短可能被限流，太长影响响应速度
LIBLIB_POLL_INTERVAL=5000

# 请求超时时间 - 单次HTTP请求超时，毫秒 (5000-30000)
# 网络不稳定时可适当增加
LIBLIB_REQUEST_TIMEOUT=10000

# 额外网络配置 - LoRA、Embedding等扩展模型
# 格式：<lora:model_name:weight> 或 <embedding:name>
# 示例：<lora:anime_style:0.8>,<lora:detail_tweaker:0.5>
LIBLIB_EXTRA_NETWORK=

# 触发词 - 激活特定模型特性的关键词
# 某些lora模型需要特定触发词才能发挥最佳效果
# 多个触发词用逗号分隔
LIBLIB_TRIGGER_WORDS=
