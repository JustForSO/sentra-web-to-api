# Kusa AI 图像生成供应商配置

# 授权令牌 (Bearer token) - 必填
# 从您的 Kusa 账户设置中获取此令牌
KUSA_AUTHORIZATION=your_bearer_token_here

# 图像尺寸 (可选，默认为 2048x2048)
# 支持的尺寸范围通常为 512-2048 像素
KUSA_WIDTH=1024
KUSA_HEIGHT=1024

# 风格ID (可选，默认: 40)
# 不同的风格ID会产生不同的艺术风格效果
KUSA_STYLE_ID=40

# 生成图像数量 (可选，默认: 1)
# 单次请求可生成的图像数量，设置为1-4之间
KUSA_AMOUNT=1

# 可选的Cookie (可选)
# 仅在您的账户需要额外会话验证时使用
# 通常情况下不需要设置此项
KUSA_COOKIE=your_session_cookie_here
